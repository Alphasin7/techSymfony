{% extends 'base.html.twig' %}

{% block title %}Conge{% endblock %}

{% block body %}
    <h1>Conge</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conge.id }}</td>
            </tr>
            <tr>
                <th>Date_debut_con</th>
                <td>{{ conge.dateDebutCon ? conge.dateDebutCon|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin_con</th>
                <td>{{ conge.dateFinCon ? conge.dateFinCon|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree_conge</th>
                <td>{{ conge.dureeConge }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ conge.reason }}</td>
            </tr>
            <tr>
                <th>Progress</th>
                <td>{{ conge.progress }}</td>
            </tr>
            <tr>
                <th>Department</th>
                <td>{{ conge.department }}</td>
            </tr>
            <tr>
                <th>Fullname</th>
                <td>{{ conge.fullname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_conge_index') }}">back to list</a>

    <a href="{{ path('app_conge_edit', {'id': conge.id}) }}">edit</a>

    {{ include('conge/_delete_form.html.twig') }}
{% endblock %}
