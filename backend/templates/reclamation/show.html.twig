{% extends 'base.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
    <h1>Reclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reclamation.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ reclamation.contenu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fullname</th>
                <td>{{ reclamation.fullname }}</td>
            </tr>
            <tr>
                <th>Department</th>
                <td>{{ reclamation.department }}</td>
            </tr>
            <tr>
                <th>Progress</th>
                <td>{{ reclamation.progress }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ reclamation.position }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ reclamation.answer }}</td>
            </tr>
            <tr>
                <th>Problem</th>
                <td>{{ reclamation.problem }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reclamation_index') }}">back to list</a>

    <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}">edit</a>

    {{ include('reclamation/_delete_form.html.twig') }}
{% endblock %}
